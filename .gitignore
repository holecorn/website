# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Per-machine Claude Code permissions — note `*.local` above does not match it.
# `.claude/rules/` is deliberately tracked; it is shared instruction, not config.
.claude/settings.local.json

# Downloaded for the firmware host tests (see firmware/hub75/README.md)
firmware/hub75/host/ArduinoJson.h

# The board's WiFi and broker credentials. This repo is public, so this file must
# never be tracked — firmware/hub75/secrets.example.h is the tracked template.
firmware/hub75/secrets.h

# Renders from tools/ (see tools/README.md)
tools/out/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Renders from the HUB75 host renderer (see firmware/hub75/README.md)
firmware/hub75/out/
